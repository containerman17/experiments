# 2026-04-28

## ~22:22 (prev day, rolled into today's session)

- New task on `current.md`: sync subnet
  `j6HXQ…veFV` (chain `2SfshB…WY`, ~333k blocks) on a *fresh*
  local avalanchego, then probe whether deleting only the subnet-evm
  DB triggers re-execution from the avalanchego shared DB
  (ProposerVM containers).
- Created branch `deforestation/subnet-track-experiment` in
  `~/avalanchego` off latest master.
- Started node via `go run ./main` inside tmux session
  `deforest-avago`. Flags: `--data-dir=~/deforestation-avago`,
  `--http-port=9660`, `--staking-port=9661`,
  `--partial-sync-primary-network=true`,
  `--track-subnets=j6HXQ…veFV`.
- Ports `9660-9661` chosen to avoid the running `avago` (9652-9653)
  and `avago-mainnet` (9650-9651) containers — kept those untouched.

## ~00:57

- P-chain bootstrap status: fetch phase done (~1.5h, all 24.86M
  blocks on disk), executing phase ~46% complete (ETA ~2h).
- Shared DB at `~/deforestation-avago/db/mainnet/` is 50 GB so far.
- `chainData/` only has the P-chain dir; subnet chain not yet
  created (waits for P-chain bootstrap to finish).
- Set up persistent monitor (5% buckets + phase transitions) so we
  get notified at meaningful milestones rather than every tick.

## ~01:01

- Filed `wiki/avalanchego-on-disk-layout.md` documenting:
  - shared node DB lives at `<data-dir>/db/<network-name>`
  - per-chain dir at `<data-dir>/chainData/<chainID>/`
    (`chains/manager.go:486`)
  - subnet-evm standalone DB at `<chainDataDir>/db`
    (`subnet-evm/plugin/evm/vm_database.go:160`)
  - ProposerVM container index lives in the *shared* DB, NOT in
    `chainData/<chainID>/`
- Updated `current.md` step 7 to point at the precise path that
  step deletes (`chainData/2SfshB…WY/db/`), and moved three
  questions from "open" to "resolved" (DB layout, `go run`
  viability, P-chain bootstrap timing on this hardware).

## ~01:50

- Filed `wiki/proposervm-startup-repair.md` documenting
  `repairAcceptedChainByHeight` (avalanchego/vms/proposervm/vm.go
  :610-681). Key finding: ProposerVM treats inner-ahead-of-pro as
  fatal but pro-ahead-of-inner as routine — it rolls itself back
  to match inner. Same Pebble-ahead-OK pattern as our own
  Pebble↔Firewood ordering decision (decisions.md 2026-04-27).

## ~02:54 — P-chain bootstrap completed

- P-chain finished: `numExecuted: 24859314`, `duration: 3h16m51s`,
  `lastAcceptedHeight: 24860215`.
- Subnet chain creation **failed** immediately:
  `error while getting vmFactory: "WFhM9Gmc...cLSf" was not found`.
  The expected vmID for this subnet is
  `WFhM9GmcSXUULSJj9kpUoph23gLMsEvrBhwwS4kz29zFBcLSf` (the subnet
  registered subnet-evm under a custom vmID, different from
  subnet-evm's `DEFAULT_VM_ID = srEXi…6X7Dy`).
- `~/deforestation-avago/plugins/` was empty (we never built/
  installed the subnet-evm plugin).
- Existing built binary at `~/.avalanchego/plugins/srEXi…6X7Dy`
  was a broken symlink to a deleted tmp build.
- Built subnet-evm fresh from `~/subnet-evm` on branch
  `slimarchive` (which has the user's archival features:
  "store traces for pruned nodes" + "enable archival mode for
  Firewood" — this is the variant we actually want exercised).
  `go build -o /tmp/subnet-evm-build/subnet-evm ./plugin/`
  succeeded, 63 MB binary.
- Copied to
  `~/deforestation-avago/plugins/WFhM9GmcSXUULSJj9kpUoph23gLMsEvrBhwwS4kz29zFBcLSf`.
- Sent Ctrl-C to tmux, node shut down cleanly. Restarted at
  ~02:58.
- Restart attempt #1 hit a second mismatch: **RPCChainVM
  protocol version mismatch** — avalanchego v1.14.2 (master)
  expects protocol 45, our subnet-evm slimarchive plugin emits
  44. Slimarchive go.mod pins
  `ava-labs/avalanchego v1.14.1-0.20251111165133-29b4e6bc541b`,
  i.e. commit `29b4e6bc54` from 2025-11-11.
- Considered downgrading the avalanchego binary and reusing the
  3h+ leveldb. Risk: 5474 commits between pin and master =
  schema incompatibility likely.
- Mitigation that worked: `git worktree add /tmp/avago-pinned
  29b4e6bc54` to build avalanchego at the pinned commit *without
  disturbing* our `deforestation/subnet-track-experiment`
  branch. Restarted node from `/tmp/avago-pinned` against the
  same data dir.
- **Pinned avalanchego v1.14.0 read the v1.14.2-written leveldb
  with no migration error.** P-chain came up at
  `lastAcceptedHeight: 24860234`. ProposerVM init "after fork"
  also fine. Peers visible as "newer version" but handshake OK.
  So leveldb is forward+backward compatible across this 5-month
  span. Worth keeping in mind for future version-juggling.

## ~03:10 — subnet bootstrap completed; baseline recorded

- Subnet bootstrap timeline (cold network sync):
  - fetch 327,966 blocks: ~3m20s
  - execute 327,966 blocks: ~3m45s
  - total subnet bootstrap: ~7m05s
- Tip via subnet RPC: `eth_blockNumber = 0x5012e` (327,950).
- Disk baseline (`wiki/subnet-2SfshB-baseline-size.md`):
  avalanchego shared DB 45 GB, subnet-evm DB 1.9 GB, full data
  dir 47 GB. Disk free 164 GB / 2.4 TB (94% used) — getting
  tight; flagged for later.
- P-chain bootstrap timing (cold): fetch ~1h30m, execute
  3h16m51s, total ~4h47m. Subnet bootstrap dwarfed by P-chain
  ~40:1.
- Ready for steps 7-8 (wipe subnet-evm DB, restart, observe
  whether local-container-cache validates the user's gut
  feeling).

## ~02:35

- User asked, given the wipe-and-restart pattern they want, what
  practically happens — does ProposerVM "refeed" the blocks?
  Initial answer: no, the standard flow re-bootstraps from peers.
- User: gut feeling that some cache will kick in, told me to run
  the experiment anyway.
- Pre-staged step-6/7/8 commands as a "Commands" section in
  current.md so the post-sync work is reproducible (baseline
  measurements, surgical mv-not-rm of the subnet-evm DB,
  start/elapsed/end timing for the re-sync).
- Then double-checked the bootstrapper code and **the user's gut
  feeling is right at the source level**: in
  `bootstrapper.go:405-417`, `startSyncingBlocks` calls
  `b.VM.GetBlock(blkID)` first and only falls back to network
  `fetch` if that fails. ProposerVM.GetBlock is a plain
  container-store lookup
  (`vm.go:780, vm.State.GetBlock(blkID)`); not gated by
  lastAccepted. So orphan containers in `db/mainnet/` *do* serve
  as a cache during re-bootstrap.
- Filed `wiki/bootstrap-local-container-cache.md` with the
  citations. Rewrote `current.md` step 8 with the updated
  expected behaviour: bandwidth ~zero, CPU still pays full block
  re-execution (the dominant cost for an EVM chain anyway).
