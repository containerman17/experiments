-- Auto-generated by cmd/remoteschema/schema.go - DO NOT EDIT

-- default.chain_status
CREATE TABLE default.chain_status
(
    `chain_id` UInt32,
    `name` String,
    `last_updated` DateTime64(3, 'UTC'),
    `last_block_on_chain` UInt64
)
ENGINE = ReplacingMergeTree(last_updated)
PRIMARY KEY chain_id
ORDER BY chain_id
SETTINGS index_granularity = 8192;

-- default.raw_blocks
CREATE TABLE default.raw_blocks
(
    `chain_id` UInt32,
    `block_number` UInt32,
    `hash` FixedString(32),
    `parent_hash` FixedString(32),
    `block_time` DateTime64(3, 'UTC'),
    `miner` FixedString(20),
    `difficulty` UInt8,
    `total_difficulty` UInt64,
    `size` UInt32,
    `gas_limit` UInt32,
    `gas_used` UInt32,
    `base_fee_per_gas` UInt64,
    `block_gas_cost` UInt64,
    `state_root` FixedString(32),
    `transactions_root` FixedString(32),
    `receipts_root` FixedString(32),
    `extra_data` String,
    `block_extra_data` String,
    `ext_data_hash` FixedString(32),
    `ext_data_gas_used` UInt32,
    `mix_hash` FixedString(32),
    `nonce` LowCardinality(FixedString(8)),
    `sha3_uncles` FixedString(32),
    `uncles` Array(FixedString(32)),
    `blob_gas_used` UInt32,
    `excess_blob_gas` UInt64,
    `parent_beacon_block_root` LowCardinality(FixedString(32)),
    `min_delay_excess` UInt64
)
ENGINE = MergeTree
ORDER BY (chain_id, block_number)
SETTINGS index_granularity = 8192;

-- default.raw_logs
CREATE TABLE default.raw_logs
(
    `chain_id` UInt32,
    `address` FixedString(20),
    `block_number` UInt32,
    `block_hash` FixedString(32),
    `block_time` DateTime64(3, 'UTC'),
    `transaction_hash` FixedString(32),
    `transaction_index` UInt16,
    `log_index` UInt32,
    `tx_from` FixedString(20),
    `tx_to` Nullable(FixedString(20)),
    `topic0` FixedString(32),
    `topic1` Nullable(FixedString(32)),
    `topic2` Nullable(FixedString(32)),
    `topic3` Nullable(FixedString(32)),
    `data` String,
    `removed` Bool
)
ENGINE = MergeTree
ORDER BY (chain_id, block_time, address, topic0)
SETTINGS index_granularity = 8192;

-- default.raw_traces
CREATE TABLE default.raw_traces
(
    `chain_id` UInt32,
    `tx_hash` FixedString(32),
    `block_number` UInt32,
    `block_time` DateTime64(3, 'UTC'),
    `transaction_index` UInt16,
    `trace_address` Array(UInt16),
    `from` FixedString(20),
    `to` Nullable(FixedString(20)),
    `gas` UInt32,
    `gas_used` UInt32,
    `value` UInt256,
    `input` String,
    `output` String,
    `call_type` LowCardinality(String),
    `tx_success` Bool,
    `tx_from` FixedString(20),
    `tx_to` Nullable(FixedString(20))
)
ENGINE = MergeTree
ORDER BY (chain_id, block_number)
SETTINGS index_granularity = 8192;

-- default.raw_txs
CREATE TABLE default.raw_txs
(
    `chain_id` UInt32,
    `hash` FixedString(32),
    `block_number` UInt32,
    `block_hash` FixedString(32),
    `block_time` DateTime64(3, 'UTC'),
    `transaction_index` UInt16,
    `nonce` UInt64,
    `from` FixedString(20),
    `to` Nullable(FixedString(20)),
    `value` UInt256,
    `gas_limit` UInt32,
    `gas_price` UInt64,
    `gas_used` UInt32,
    `success` Bool,
    `input` String,
    `type` UInt8,
    `max_fee_per_gas` Nullable(UInt64),
    `max_priority_fee_per_gas` Nullable(UInt64),
    `priority_fee_per_gas` Nullable(UInt64),
    `base_fee_per_gas` UInt64,
    `contract_address` Nullable(FixedString(20)),
    `access_list` Array(Tuple(
        address FixedString(20),
        storage_keys Array(FixedString(32))))
)
ENGINE = MergeTree
ORDER BY (chain_id, block_number)
SETTINGS index_granularity = 8192;

-- default.sync_watermark
CREATE TABLE default.sync_watermark
(
    `chain_id` UInt32,
    `block_number` UInt32
)
ENGINE = EmbeddedRocksDB
PRIMARY KEY chain_id;
